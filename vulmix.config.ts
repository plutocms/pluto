import type { VulmixConfig } from "vulmix/types/vulmix-config";

module.exports = <VulmixConfig>{
  lang: 'pt-BR',
  head: {
    meta: [
      {
        name: 'title',
        content: 'Pluto CMS',
      }
    ]
  }
};
